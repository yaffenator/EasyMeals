# Team Report — 20260226

## Last Week’s Progress
- Implemented the website’s authentication system (Google & GitHub OAuth).
- Implemented Gemini into the backend to create recipes on the dashboard.
- Began full implementation of Gemini with the frontend through FastAPI.
- Finished developing the rating & Bayesian score system, and created the structured filtering layer to ensure the backend returns only valid meals for a user request.

## Progress & Issues (What you did / what worked / what you learned / where you had trouble / where you are stuck)
- Implemented recommendation algorithms for recipes in the database.
- Set up automated testing through GitHub Actions with Pytest, React Testing Library, and Jest.
- Implemented basic user auth with email and password.

## Plans & Goals for Next Week
- **Dean:** Allow Gemini to write directly to the Firestore database (last major step before fine-tuning our model).
- **Dylan:** Finish adding the response from Gemini to the database and storing the data correctly; fix incorrect pricing; optimize prompt/model for faster generation; finish the reroll feature.
- **Noam:** Connect the user authentication token to our database; transfer questionnaire logic from using JSON token to a database attribute.
- **Wade (Ayowade):** Develop the structured filtering layer to ensure the backend returns valid meals for a user request; start tying everything built so far together to build the core functionality of the app.

---

# Individual Contributions

## Dean Leon

### Last Week’s Progress
- Set up React Testing Library/Jest for frontend testing through GitHub Actions.
- Tests will be run during pull requests and pushed to the main branch.
- Created 4 test `.tsx` files to cover the 5 test types required in Project Milestone 5.
- Assisted Dylan with modifying the database to better fit Gemini’s output.

### Progress & Issues
When first implementing React Testing Library, I had no idea what Jest was, but I saw online that many developers prefer pairing the two tools together. I found more documentation online about how to use them together rather than RTL by itself, so I chose to implement it as well.

Aside from the small demo we did in class, I had never worked with GitHub Actions before, and it was a little confusing to set up. However, Ayowade had set up Pytest for the backend testing, and I was able to follow his setup and modify it for RTL and Jest.

### Plans & Goals for Next Week
- Allow Gemini to write directly to the Firestore database (~2 hours).

---

## Dylan Peniuk

### Last Week’s Progress
- Completed the prompt sent to Gemini to create the meal plan based off the information from the JSON created when filling out the questionnaire about meal preferences.
- Created functionality for getting Gemini’s response about the created meal plan in a JSON format that reflects our database.
- Created a button for rerolling meals.

### Progress & Issues
I learned a lot about how to call the Gemini API and how it prompts and reruns information. I was able to format its response into a JSON file that we will be able to use for the database. It is complicated, so there is still work to be done because it is not super accurate. I may need to adjust how I prompt Gemini and use the user’s location for specific pricing.

### Plans & Goals for Next Week
- Finish adding the response from Gemini to the database and storing the data correctly.
- Fix the incorrect pricing that it gives.
- Optimize the prompt and model for faster generation.
- Finish the reroll feature.

---

## Noam Yaffe

### Last Week’s Progress
- Implemented a fully-working email/password authentication system for both creating an account and logging into a pre-existing one.
- Added options for the user to use Google & GitHub OAuth to log in or create their account.

### Progress & Issues
Over the last week, I learned how to initialize Firebase authentication and connect it to our project. By going through this process, I learned a lot about the Firebase authentication system as a whole, but also ran into a few issues during its initialization. For example, while following a specific tutorial on setting up Firebase Auth in our database, I accidentally refactored our entire codebase’s setup. Through this, I also learned about GitHub's “reroll commit” feature, which allowed me to revert to the previously working version of our codebase.

Overall, this last week has taught me a lot about Firebase as a whole, and about cool features that GitHub offers that I will be sure to use in my future projects.

### Plans & Goals for Next Week
- Connect user authentication token to our database, creating a new entry into the `Users` collection every time a new user creates an account (~1–2 hours).
- Transfer questionnaire logic from using JSON token to a database attribute to determine whether or not the user has taken the quiz (e.g., a boolean named `takenQuestionnaire`) (~1 hour).
- Start talking with the backend team regarding how their work could potentially be connected to the frontend that Dylan and I created.

---

## Ayowade Owojori

### Last Week’s Progress
- Developed the rating and Bayesian score system, the diversity algorithm, and weighted meal sampling in the backend.
- Made CI tests for the backend using a mock database.

### Progress & Issues
I made the rating and Bayesian score system, the diversity algorithm, and weighted meal sampling features, and made PRs for them. The rating and Bayesian score system was successfully merged. Additionally, I made tests to ensure the functionality worked as intended.

### Plans & Goals for Next Week
- Develop the structured filtering layer to ensure the backend returns only valid meals for a user request (~2–3 hours).
- Start tying everything built so far together to build the core functionality of the app (~3–4 hours).
